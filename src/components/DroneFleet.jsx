export default function DroneFleet({ drones }) { return <section className="card drones"><div className="panel-heading"><div><span className="eyebrow">DRONE FLEET</span><h3>Field availability</h3></div></div>{drones.map(([id,status]) => <div className="drone" key={id}><b>Drone {id}</b><span className={`badge ${status === 'Available' ? 'normal' : status === 'Offline' ? 'critical' : 'high'}`}>{status}</span></div>)}<button className="secondary-button">VIEW DRONE FLEET</button></section> }
