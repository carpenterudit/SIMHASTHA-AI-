import { useState } from 'react'
export default function AlertPanel({ alerts }) { const [active, setActive] = useState(alerts); return <section className="card alerts"><div className="panel-heading"><div><span className="eyebrow">SYSTEM ALERTS</span><h3>Recent notifications</h3></div><span className="count">{active.length} new</span></div>{active.length ? active.map((alert,index) => <div className="alert" key={alert}><i className={index === 1 ? 'critical' : 'moderate'}/><span>{alert}</span><button onClick={() => setActive(active.filter(a => a !== alert))}>Acknowledge</button></div>) : <p className="empty">All alerts acknowledged.</p>}</section> }
