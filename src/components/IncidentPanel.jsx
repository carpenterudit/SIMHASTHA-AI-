const colors = { Critical: 'critical', High: 'high', Moderate: 'moderate' }
export default function IncidentPanel({ incidents }) { return <section className="card incidents"><div className="panel-heading"><div><span className="eyebrow">ACTIVE INCIDENTS</span><h3>Needs operator attention</h3></div><span className="count">{incidents.length} open</span></div>{incidents.map(i => <article className="incident" key={i.location}><i className={colors[i.severity]}/><div><span className={`badge ${colors[i.severity]}`}>{i.severity}</span><b>{i.location}</b><p>{i.text}</p><small>{i.time}</small></div><button className="text-button">View</button></article>)}</section> }
