import { useState } from 'react'
import Header from '../components/Header'; import Sidebar from '../components/Sidebar'; import KpiCard from '../components/KpiCard'; import CctvPanel from '../components/CctvPanel'; import UjjainMap from '../components/UjjainMap'; import CrowdChart from '../components/CrowdChart'; import ZoneTable from '../components/ZoneTable'; import IncidentPanel from '../components/IncidentPanel'; import EmergencyWidget from '../components/EmergencyWidget'; import DroneFleet from '../components/DroneFleet'; import AlertPanel from '../components/AlertPanel'; import { dashboardData } from '../data/mockData'
export default function Dashboard() { const [menu, setMenu] = useState(false); const [modal, setModal] = useState(false); return <div className="app-shell"><Header onMenu={() => setMenu(!menu)}/><Sidebar open={menu} onClose={() => setMenu(false)}/><main><div className="page-intro"><div><p className="eyebrow">OPERATIONAL OVERVIEW</p><h1>Command Center Dashboard</h1><span>Simhastha Ujjain · Live mock operational data</span></div><button className="refresh">● Auto-refresh enabled</button></div><div className="kpi-grid">{dashboardData.kpis.map(item => <KpiCard item={item} key={item.label}/>)}</div><div className="primary-grid"><CctvPanel/><UjjainMap/></div><div className="content-grid"><div><CrowdChart data={dashboardData.chart}/><ZoneTable zones={dashboardData.zones}/></div><div className="side-stack"><IncidentPanel incidents={dashboardData.incidents}/><EmergencyWidget onOpen={() => setModal(true)} open={modal} onClose={() => setModal(false)}/><DroneFleet drones={dashboardData.drones}/><AlertPanel alerts={dashboardData.alerts}/></div></div></main></div> }
